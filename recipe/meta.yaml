{% set name = "scikit-plot" %}
{% set version = "0.3.3" %}
{% set sha256 = "b18e121535a0d2cfdde268c8c0437774da572e38474b84eed3b09d710572fae6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - matplotlib
    - scikit-learn
    - scipy
    - joblib

test:
  imports:
    - scikitplot
    - scikitplot.plotters
    - scikitplot.helpers
    - scikitplot.classifiers
    - scikitplot.clustering
    - scikitplot.estimators
    - scikitplot.cluster
    - scikitplot.decomposition
    - scikitplot.metrics

about:
  home: https://github.com/reiinakano/scikit-plot/
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'An intuitive library to add plotting functionality to scikit-learn objects.'
  doc_url: scikit-plot.readthedocs.io/

extra:
  recipe-maintainers:
    - ericmjl
    - reiinakano
